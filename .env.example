# Copy to .env (never commit secrets) or export in the shell.

# Optional LLM judge (only if VAL_METHOD=reward)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
GEMINI_API_KEY=

# Optional logging
SWANLAB_API_KEY=
HF_TOKEN=

# Models (HF ids or local paths)
MODEL_PATH=Qwen/Qwen3-1.7B
GUARD_MODEL_PATH=meta-llama/Llama-Guard-3-8B

# Optional local safety-eval suite (directory path)
EVAL_LLM_SAFETY_DIR=
