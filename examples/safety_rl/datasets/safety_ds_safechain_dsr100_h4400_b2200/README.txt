SafeChain subset used in the paper (teacher DSR-filtered).

  harmful kept: 4400
  benign kept:  2200
  train.jsonl / val.jsonl / val_wjwc_mix.jsonl

Train with:

  DATA_SOURCE=safechain bash safety_opsd_train.sh
